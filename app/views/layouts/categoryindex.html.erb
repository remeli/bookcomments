<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%= render("layouts/head") %>
</head>
<body>
  <%= render('shared/head_menu') %>
  <div class="container">
    <%= render('layouts/header') %>
    <%= render('layouts/menu') %>
    <div class="row" id="content-block">
      <div class="twelve columns" id="categories-layout">
        <%= yield %>
      </div>
    </div>
    <%= render('layouts/footer') %>
  </div>
</body>
</html>